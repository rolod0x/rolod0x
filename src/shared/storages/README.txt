This storage mechanism is from:

https://github.com/Jonghakseo/chrome-extension-boilerplate-react-vite

However it is not used and is only kept so that we can keep the sidebar
boilerplate around for when that may come in useful.

Instead, storage is handled by src/shared/options-storage.ts.
